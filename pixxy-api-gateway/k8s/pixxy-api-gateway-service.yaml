apiVersion: v1
kind: Service
metadata:
    name: pixxy-api-gateway
    namespace: pixxy
spec:
    type: ClusterIP
    selector:
        app: pixxy-api-gateway
    ports:
        - port: 80
          targetPort: 8080
